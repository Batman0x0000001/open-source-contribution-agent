# Required: Anthropic API Key for AI agent operations
ANTHROPIC_API_KEY=

# Optional: Use alternative Anthropic-compatible API endpoint (e.g., DeepSeek)
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic

# Required: Model identifier accepted by Anthropic or the configured compatible gateway
MODEL_ID=deepseek-v4-pro

# IMPORTANT: GitHub Personal Access Token
# Without token: 60 API requests/hour (easily exceeded)
# With token: 5,000 API requests/hour
# Get your token at: https://github.com/settings/tokens
# Permissions needed: None (public_repo read access is default)
GITHUB_TOKEN=

# Agent behavior limits
OSC_AGENT_MAX_ROUNDS=30
OSC_AGENT_MAX_TOKENS=200000
OSC_AGENT_DEADLINE_SECONDS=1800
OSC_AGENT_REPEAT_ACTION_LIMIT=3
OSC_AGENT_FAILURE_LIMIT=3
OSC_AGENT_NO_PROGRESS_LIMIT=6
OSC_AGENT_MODEL_MAX_ATTEMPTS=3
OSC_AGENT_MODEL_RETRY_BASE_SECONDS=1
OSC_AGENT_MODEL_RETRY_MAX_SECONDS=8
OSC_AGENT_MAX_CHANGED_FILES=5
OSC_AGENT_MAX_DIFF_LINES=400

# Extra non-sensitive environment variable names passed to Tool subprocesses.
# Credentials and names containing token/secret/password markers are always blocked.
OSC_AGENT_SUBPROCESS_ENV_ALLOWLIST=[]

# Optional GitHub App bot (install this project with the `bot` extra).
# Keep Control-only credentials out of the Worker environment file in production.
OSC_AGENT_GITHUB_APP_ID=
OSC_AGENT_GITHUB_APP_PRIVATE_KEY_PATH=
OSC_AGENT_GITHUB_WEBHOOK_SECRET=
OSC_AGENT_GITHUB_PUBLIC_URL=
OSC_AGENT_BOT_DATABASE_PATH=
OSC_AGENT_BOT_WORKSPACE_ROOT=
OSC_AGENT_BOT_REPOSITORIES_CONFIG=
OSC_AGENT_BOT_BIND_HOST=127.0.0.1
OSC_AGENT_BOT_BIND_PORT=8080
OSC_AGENT_BOT_WORKER_ID=
OSC_AGENT_BOT_MAX_CONCURRENT_JOBS=1
OSC_AGENT_GITHUB_COMMIT_NAME=
OSC_AGENT_GITHUB_COMMIT_EMAIL=
